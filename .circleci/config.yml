---
version: "2"
jobs:
  build-snapshot:
    docker:
    - image: "gradle:4.10.3"
    steps:
    - checkout:
        path: "."
    - run:
        command: |-
          gradle check
          gradle build
  trigger-promotion:
    docker:
    - image: "gradle:4.10.3"
workflows:
  version: "2"
  default:
    jobs:
    - build-snapshot:
        filters:
          branches:
            only:
            - "master"
    - trigger-promotion:
        requires:
        - "build-snapshot"
