def proj = "philarios"
def modules = [
        "gradle-v0",
        "core-v0",
        "schema-v0",
        "concourse-v0",
        "concourse-example-v0",
]

modules.each {
    include(it)
    project(":$it").name = "$proj-${it.replaceAll("/", "-")}"
}
