# CircleCI

## Attributions
The CircleCI schema file `circleciconfig_original.json` was taken from http://json.schemastore.org/circleciconfig. I had
to modify it so that it adheres to the limited subset of JSON schema that Philarios currently supports.